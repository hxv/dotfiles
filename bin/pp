#!/usr/bin/env bash

FILES=$(path-extractor)

if [[ "$FILES" == "" ]]; then
    exit
fi

SELECTED=$(echo "$FILES" | fzf -m --height=50%)

if [[ "$SELECTED" == "" ]]; then
    exit
fi

"$@" $SELECTED <&1
